mixin scroll-down(href)
    .scroll-down
        a.scroll-down__link(href=`#${href}` data-scroll-to=`#${href}`)
            span.scroll-down__text Скролл
            +icon('scroll', 'scroll-down__icon')