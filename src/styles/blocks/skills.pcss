.skills__list {
    display: flex;
    flex-direction: column;
    align-items: center;
}

.skills__title {
    font-size: 24px;
    font-weight: 700;
    margin-bottom: 40px;
}

.skills__item {
    margin-bottom: 50px;
    &:last-of-type {
        margin-bottom: 0;
    }
}